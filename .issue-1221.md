# [mobile] Optimize data table column visibility for portrait tablet orientation

## Description

[mobile] Optimize data table column visibility for portrait tablet orientation

This feature enhances the Soroban CrashLab dashboard by adding new functionality that improves the user experience or extends the platform capabilities.

## Acceptance Criteria

- [ ] Feature is implemented and functional
- [ ] UI follows the Navy Professional design system (CSS variables, dark/light mode)
- [ ] Responsive on mobile and desktop viewports
- [ ] Proper loading, empty, and error states are handled
- [ ] No console errors or warnings

## Possible Files to Create or Modify

- New page component in `apps/web/src/app/` if adding a new route
- Utility functions in `apps/web/src/app/*-utils.ts` or `apps/web/src/lib/`
- Shared components in `apps/web/src/components/` if reusable
- Mock data in `apps/web/src/fixtures/` if needed
- API routes in `apps/web/src/app/api/` if backend support is needed
- Corresponding test files

## Verification

- [ ] Run `npm run lint` — must pass with 0 errors
- [ ] Run `npm run build` — must compile successfully
- [ ] Run `npm test` — all existing tests must pass
- [ ] Manual testing in both light and dark mode
- [ ] Manual testing on mobile viewport (320px, 768px, 1024px)

## CI Guidance

All GitHub Actions workflows must pass before merging:
- `web` job: lint + test + build
- `e2e` job: Playwright tests
- Secrets scan with gitleaks

## Contribution Tips

- Check similar existing implementations in the codebase for patterns
- Use the existing CSS variable system (`var(--bg)`, `var(--surface)`, etc.) for styling
- Follow the existing file naming conventions (`*-utils.ts` for logic, `*.test.ts` for tests)
- Ensure all new UI components handle both light and dark themes

---

## PR Checklist
- [ ] Changes address the issue requirements
- [ ] `npm run lint` passes
- [ ] `npm run build` compiles
- [ ] `npm test` passes

Closes #1221