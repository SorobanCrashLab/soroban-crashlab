import ChartBuilderPage from '@components/ChartBuilderPage';

export const metadata = {
  title: 'Custom Chart Builder | Soroban CrashLab',
  description: 'Build custom charts from run data with configurable metrics, dimensions, and chart types.',
};

export default function Page() {
  return <ChartBuilderPage />;
}
